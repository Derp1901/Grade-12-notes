#Derivation #Mag1 
Consider a current carrying conductor with $I$. Consider a small current element of length $dl$, at a distance $r$ from the current element, let the current element make an angle $\theta$ between the current element and line joining. 

The magnetic field applied on the point but the current element $dB\propto$ current $I$,$\propto$length of current element, $\propto \sin \theta,\propto \frac{1}{r^2}$ the distance between point and current element.

$$\begin{align}
dB&\propto \frac{Idl\sin\theta}{r^2}\\
db&=\frac{\mu_{o}}{4\pi}.\frac{}
\end{align}$$